public class trappingRainWater {
    

    public static int trappedWater(int[] nums){
        //Brute force
        // int n=nums.length;
        // int Lmax = 0;
        // int Rmax = 0;
        // int minValue;
        // int Twater = 0;
        // for(int i = 1; i<=n-2; i++){
        //     for(int j = 0; j<i; j++){
        //          Lmax = Math.max(Lmax, nums[j]);
        //     }
        //     for(int j = n-1; j>i; j--){
        //         Rmax = Math.max(Rmax, nums[j]);
        //     }
        //     minValue = Math.min(Lmax, Rmax);
        //     Twater += minValue-nums[i];

        // }

        


        // optimized
        // int n = nums.length;
        // int Twater = 0;
        // int[] Lmax = new int[n];
        // Lmax[0] = nums[0];
        // int[] Rmax = new int[n];
        // Rmax[n-1] = nums[n-1];
        // for(int i = 1; i<n; i++){
        //     Lmax[i] = Math.max(Lmax[i-1], nums[i]);
        // }
        // for(int i = n-2; i>=0; i--){
        //     Rmax[i] = Math.max(Rmax[i+1], nums[i]);
        // }
        // for(int i = 0; i<n; i++){
        //     Twater += Math.min(Lmax[i], Rmax[i])-nums[i];
        // }
        


        //Extra optimized
        int  n = nums.length;
        int low =1;
        int high = n-2;
        int Lmax =nums[0];
        int Rmax =nums[n-1];
        int Twater = 0;
        while(low <= high){
            Lmax = Math.max(Lmax, nums[low]);
            Rmax = Math.max(Rmax, nums[high]);
            if(Lmax < Rmax){
                Twater += Lmax-nums[low];
                low++; 
            }else{
                Twater += Rmax-nums[high];
                high--;
            }

        }



        return Twater;
    }
    public static void main(String[] args) {
        
        int[] arr1 = {0,1,0,2,1,0,1,3,2,1,2,1};
        System.out.println("Trapped water = " + trappedWater(arr1));
    }
}
